# Telegram API credentials
# Get these from https://my.telegram.org/apps
TELEGRAM_API_ID=your-api-id
TELEGRAM_API_HASH=your-api-hash

# Source can be a group or channel
# For public groups/channels: use the username (e.g., @groupname)
# For private groups/channels: use the ID (e.g., -100xxxxxxxxxx)
SOURCE=source_group_or_channel_id

# Destination channel (where messages will be forwarded)
# Must be a channel where you have posting permissions
# For channels/supergroups: 
# 1. If using numeric ID: The script will automatically add -100 prefix
# 2. If using username: Include the @ symbol (e.g., @channelname)
DESTINATION_CHANNEL=-100xxxxxxxxxx # Example format for numeric ID